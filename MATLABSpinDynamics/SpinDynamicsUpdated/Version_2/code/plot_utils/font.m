% FONT
% Apply the default large axis font used by the example figures.
%
% Purpose
%   Small plotting helper used by legacy example scripts. Keeping a copy under
%   the active code tree makes the documented Version_2/code path setup
%   self-contained.
% -------------------------------------------------------------------------
set(gca, 'fontsize', 20);
