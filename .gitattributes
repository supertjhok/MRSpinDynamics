# Language-statistics overrides for GitHub Linguist.
#
# The MATLABSpinDynamics sub-tree ships one large Mathematica notebook whose
# embedded cell outputs (~5 MB of serialized text) dominate the repository's
# language bar and show up as "Wolfram Language". It is a one-off supporting
# calculation, not part of the codebase, so exclude it from the language stats.
MATLABSpinDynamics/SpinDynamics/code/circsim_new/damped_driven_harmonic_oscillator.nb linguist-vendored
